# Terraform working directories
**/.terraform/
**/.terraform.lock.hcl

# Terraform state (ห้ามขึ้น git เด็ดขาด)
*.tfstate
*.tfstate.backup
**/*.tfstate
**/*.tfstate.backup

# SSH keys / credentials
KeyPair.pem
*.pem

# Binaries
kubectl
kubectl.sha256

Nipa-SRE-intern_RC_file.sh