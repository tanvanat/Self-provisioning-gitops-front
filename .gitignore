# Terraform working directories
**/.terraform/
**/.terraform.lock.hcl

# Terraform state
terraform.tfstate*
**/terraform.tfstate*
*.tfstate
*.tfstate.*
**/*.tfstate
**/*.tfstate.*

# SSH keys / credentials
KeyPair.pem
*.pem

# Binaries
kubectl
kubectl.sha256

Nipa-SRE-intern_RC_file.sh